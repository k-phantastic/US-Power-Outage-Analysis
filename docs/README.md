# Docs site

This `docs` folder contains a Just the Docs Jekyll site adapted for the US Power Outage Analysis project.

## Local preview

```bash
cd docs
bundle install
bundle exec jekyll serve
```

## Publish on GitHub Pages

Use the `.github/workflows/pages.yml` workflow from the repository root and set GitHub Pages to use **GitHub Actions**.
